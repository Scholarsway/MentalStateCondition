{
  "name": "mentalstate",
  "version": "1.0.0",
  "private": true,
  "description": "MentalState MLP Mental Health Prediction Application",
  "scripts": {
    "build": "echo Web assets are already prepared in www",
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/core": "^7.4.3",
    "@capacitor/android": "^7.4.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3"
  }
}