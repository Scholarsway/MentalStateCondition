name: Build MentalState APK

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build-android:
    runs-on: ubuntu-latest

    steps:

      # 1. Checkout project
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. Install Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      # 3. Install Java 21
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      # 4. Install project dependencies
      - name: Install dependencies
        run: npm install

      # 5. Create Android project
      - name: Add Android platform
        run: npx cap add android

      # 6. Copy web application into Android
      - name: Sync MentalState web application
        run: npx cap sync android

      # 7. Build Android APK
      - name: Build debug APK
        working-directory: android
        run: ./gradlew assembleDebug

      # 8. Rename APK
      - name: Rename APK
        run: |
          cp android/app/build/outputs/apk/debug/app-debug.apk MentalState-debug.apk

      # 9. Upload APK
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: MentalState-debug-apk
          path: MentalState-debug.apk
